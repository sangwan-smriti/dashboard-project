// Function to navigate to a lab-specific page
function navigateToLab(labNumber) {
  alert(`Navigating to Lab-${labNumber}...`);
  // Replace the alert with actual navigation logic (e.g., window.location.href)
}
